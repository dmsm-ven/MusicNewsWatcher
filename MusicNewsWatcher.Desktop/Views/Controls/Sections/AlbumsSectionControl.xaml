<UserControl
    x:Class="MusicNewsWatcher.Desktop.Views.Controls.Sections.AlbumsSectionControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:beh="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ss="clr-namespace:LoadingSpinnerControl;assembly=LoadingSpinnerControl"
    xmlns:v="clr-namespace:MusicNewsWatcher.Desktop.Views.Controls.Items"
    xmlns:vm="clr-namespace:MusicNewsWatcher.Desktop.ViewModels.Items"
    d:DataContext="{d:DesignInstance Type={x:Type vm:ArtistViewModel},
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <ScrollViewer ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto">
        <Grid>

            <Button
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                HorizontalContentAlignment="Center"
                VerticalContentAlignment="Center"
                Panel.ZIndex="98"
                Command="{Binding GetAlbumsFromProviderForArtistCommand}"
                Style="{StaticResource ToolButtonStyle}"
                Visibility="{Binding IsUpdateAlbumsButtonVisibile, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Visible}">
                <icons:PackIconFontAwesome Kind="EyeSolid" Style="{StaticResource LargeIconSize}" />
            </Button>

            <ss:LoadingSpinner Panel.ZIndex="99" Visibility="{Binding InProgress, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Hidden}" />

            <ItemsControl
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Center"
                ItemsSource="{Binding Albums}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <v:AlbumItemControl DataContext="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel HorizontalAlignment="Center" IsItemsHost="True" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </Grid>

    </ScrollViewer>
</UserControl>
